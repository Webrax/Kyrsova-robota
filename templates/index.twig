{% extends 'base.twig' %}

{% block body %}
    <h3 class="mb-3 mt-3">Останні пости</h3>  {# build contruct for start page #}
    <div class="container">
        <div class="row">
            <div class="col-md-9">
                {% include 'section/posts.twig' with posts %}
                <div class="row">
                    <strong><font size="6"><a href="{{ url('blog') }}" class="mt-3">Дивитись все</a></font></strong> <!-- кнопка яка перекидає з сайту на блог -->
                </div>
            </div>
            <div class="col-md-3">
                {% include 'section/about.twig' %}
            </div>
        </div>
    </div>
{% endblock %}