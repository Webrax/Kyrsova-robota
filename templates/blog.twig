{% extends 'base.twig' %} {# по факту є посередником між індексом і постс.твіг #}

{% block body %}
    <h3 class="mb-3 mt-3">Останні пости</h3>  {# build contruct for blog page #}
    <div class="container">
        <div class="row">
            <div class="col-md-9">
                {% include 'section/posts.twig' with posts %}
                <div class="row">
                    {% include'section/pagination.twig' with pagination %}
                </div>
            </div>
            <div class="col-md-3">
                {% include 'section/about.twig' %}
            </div>
        </div>
    </div>
{% endblock %}